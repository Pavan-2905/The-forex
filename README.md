# The-forex
 Foreign Currency Exchange Rate Prediction. This is  full academic version of foreign exchange rate prediction Implemented using the technologies LSTM(for Training the models),FLASK(for Web-Deployment)and HTML,CSS,JS(for basic UI  ).No real time Data is used but  can also be integrated  if needed through further working on it.
